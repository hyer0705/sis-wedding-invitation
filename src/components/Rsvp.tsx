import Reveal from "./Reveal";

export default function Rsvp() {
  return (
    <Reveal>
      <div className="card">
        <div className="script-title">R.S.V.P</div>
        <p className="todo">참석 여부 폼 이식 예정 — lib/rsvp.ts (Apps Script) 연동</p>
      </div>
    </Reveal>
  );
}
