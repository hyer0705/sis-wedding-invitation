import Reveal from "./Reveal";

export default function Invitation() {
  return (
    <Reveal>
      <div className="card" style={{ padding: "46px 30px" }}>
        <div className="script-title" style={{ fontSize: 26 }}>
          Invitation
        </div>
        <p className="todo">초대글 섹션 — drafts/c.dc.html §2 이식 예정</p>
      </div>
    </Reveal>
  );
}
