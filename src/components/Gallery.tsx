import Reveal from "./Reveal";

export default function Gallery() {
  return (
    <Reveal>
      <div style={{ textAlign: "center" }}>
        <div className="script-title">Our moments</div>
        <p className="todo">갤러리 섹션 — 2열 그리드 + 라이트박스(스와이프) 이식 예정</p>
      </div>
    </Reveal>
  );
}
